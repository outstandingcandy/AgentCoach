[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "goalinsight"
version = "3.0.0"
requires-python = ">=3.12"

[project.scripts]
goalinsight = "goalinsight.cli:main"

[tool.setuptools.packages.find]
include = ["goalinsight*"]
