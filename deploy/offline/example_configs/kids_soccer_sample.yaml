# Kids / youth soccer pitch (smaller than FIFA; dims in
# configs/pitches/kids_soccer.yaml).

video:
  process_fps: 30
sample:
  stride: 1
pitch_type: kids_soccer

field_registration:
  backend: pnlcalib
  pnlcalib:
    keypoint_weights: SV_kp
    line_weights: SV_lines

device: cuda

unified_detection:
  imgsz: 0
  iou_threshold: 0.55

reid:
  backend: osnet

tracking:
  max_cosine_distance: 0.2
  vis_frames_enabled: false
  dump_yolo_raw: false
  dump_ball_diag: false

# track_consolidation needs a VLM jersey backend (claude/gemini/qwen);
# disabled by default. See futsal_sample.yaml for how to enable.

pipeline:
  stages:
    - field_registration
    - tracking
    # - track_consolidation     # needs API key — see futsal_sample.yaml
    - event_detection
    # - player_profile          # depends on track_consolidation
