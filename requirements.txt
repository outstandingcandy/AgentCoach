# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0

# Detection & Tracking
ultralytics>=8.3,<9       # YOLOv8 — pinned-major after the Dec-2024 PyPI hijack
sahi>=0.11.0              # Sliced Aided Hyper Inference for small object detection
deep-sort-realtime>=1.3,<2  # Multi-object tracking (StrongSORT compatible)

# ReID
torchreid>=0.2.5,<0.3     # Person re-identification

# Segmentation
sam2>=1.0,<2              # SAM2

# VLM for Jersey Number
qwen-vl-utils>=0.0.8,<0.1  # Qwen2.5-VL utilities

# OCR for Jersey Number Recognition (MMOCR with DBNet+SAR)
# Note: Version constraints are strict for OpenMMLab packages
mmocr==1.0.1              # MMOCR framework
mmengine>=0.8.0,<0.9.0    # MMEngine base
mmdet>=3.0.0rc5,<3.2.0    # MMDetection (required by MMOCR)
mmcv>=2.0.0rc4,<2.1.0     # MMCV utilities (requires building from source on some platforms)

# Computer Vision
opencv-python>=4.8.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Image Processing
timm>=0.9.0               # Vision models
Pillow>=10.0.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
numpy>=1.24.0
yacs>=0.1.8               # Config management (for PnLCalib)
einops>=0.6.0             # Tensor operations (optional, for clean code)

# VLM backends for track_consolidation
boto3>=1.28                # Claude Opus via AWS Bedrock
google-genai>=1.0          # Gemini via Google AI Studio
openai>=1.0.0              # Qwen via local vLLM server (OpenAI-compatible)
httpx>=0.24.0

# Annotation UI + web viewer (FastAPI + HTML/JS, no Gradio)
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0

# Testing
pytest>=7.0.0
